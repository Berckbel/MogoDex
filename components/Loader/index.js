import { View } from 'react-native';
import { styles } from './styles';

export const Loader = () => {
  return (
        <View>
            <View style={styles.loader}></View>
        </View>
  );
};

 