import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    loader: {
        minHeight: 400,
        minWidth: 400,
        borderRadius: 500,
        borderWidth: 20,
        borderTopColor: "red",
        borderColor: "blue",
    }
});